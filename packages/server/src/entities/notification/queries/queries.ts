import { myNotifications } from './myNotifications'
import { unreadNotificationCount } from './unreadNotificationCount'

export const notificationQueries = { myNotifications, unreadNotificationCount }
